# Переводы строк только LF. Иначе gofmt -l на копии с CRLF перечисляет почти
# все файлы, и проверка формата превращается в шум, который перестают читать.
* text=auto eol=lf
*.png binary
*.jpg binary
*.ttf binary
*.woff2 binary
